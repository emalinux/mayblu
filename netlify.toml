[build]
  publish = "public"
  command = "hugo"

[context.production.environment]
  HUGO_VERSION = "0.147.9"
  
[build.environment]
  HUGO_ENV = "production"

[functions]
  node_bundler = "esbuild"  
